[pytest]
DJANGO_SETTINGS_MODULE = app.settings
python_files = test*.py
# Code lives in src/ - add it to sys.path and look for tests there.
pythonpath = src
testpaths = src
addopts = --create-db

env =
  CI=1
  DEBUG=0
  SECRET_KEY=test-secret-key-at-least-32-bytes-long-for-jwt
