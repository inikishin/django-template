/.idea
/.venv
__pycache__/
*.py[cod]
# Claude Code local settings at any level (root or src/.claude while working from src).
.claude/
/CLAUDE.md

# окружение
/.env

# сборка / статика / медиа / БД (генерируются в корне, не в src)
/staticfiles/
/media/
*.sqlite3

# тесты
.pytest_cache/
.ruff_cache/
